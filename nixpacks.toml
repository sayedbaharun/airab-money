[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm ci",
  "npm --prefix server ci"
]

[phases.build]
cmds = [
  "npm --prefix server run db:push",
  "npm run build"
]

[start]
cmd = "node server/dist/index.js"
