[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm ci",
  "cd server && npm ci"
]

[phases.build]
cmds = [
  "cd server && npx prisma db push && npx prisma generate",
  "npx tsc -b",
  "npx vite build"
]

[start]
cmd = "cd server && npx tsx src/index.ts"
